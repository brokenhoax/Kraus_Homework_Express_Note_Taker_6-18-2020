-- CREATE DATABASE notes_db;

-- USE notes_db;

-- CREATE TABLE notes_db(
--   `note_id` INTEGER(11) AUTO_INCREMENT, 
--   `note_owner` VARCHAR(30) NOT NULL, 
--   `note_title` VARCHAR(30) NOT NULL,  
--   `contents` VARCHAR(150) NOT NULL, 
--   PRIMARY KEY (note_id)
--   )

-- SELECT * FROM notes_db.my_notes;
  